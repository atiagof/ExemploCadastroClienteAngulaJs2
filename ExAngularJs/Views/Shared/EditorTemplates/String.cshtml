@model String

@{
    
    var x = ViewData;
    }

<input type="text" ng-model="@(ViewData.ModelMetadata.ContainerType.Name + "." + ViewData.ModelMetadata.PropertyName)" 
    
    @(ViewData.ModelMetadata.IsRequired? "required" : "")
     />